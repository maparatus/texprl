# Development

Clone this repo and run the following steps:

1.  `npm install`
2.  `npm run build:grammar` — you'll need to run the again if you change `./parser/lang.grammar`
3.  `npm run dev`

Go to <http://localhost:8000/demo/> to see a demo.
